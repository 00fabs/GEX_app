{
  "$schema": "https://schema.railpack.com",
  "deploy": {
    "startCommand": "streamlit run app.py --server.address 0.0.0.0 --server.port ${PORT:-8501} --server.fileWatcherType none --browser.gatherUsageStats false --server.headless true"
  }
}
